// The project list the app renders and the RAG index embeds.
//
// It's fully AUTO-GENERATED (src/data/projects.generated.ts): every public repo
// that has a detailed README, discovered by scripts/build-projects.ts on each
// build / CI run. Add or improve a repo's README and it appears automatically;
// empty / stub repos are filtered out. No manual curation needed.
import { generatedProjects } from './projects.generated'
export { DOMAINS } from './types'

export const projects = generatedProjects
